apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# T24 — Observabilidade: identificação de gargalos (latência de dependência via mesh)
resources:
  - ../../base

patches:
  - path: virtualservice-delay.yaml
    target:
      group: networking.istio.io
      version: v1beta1
      kind: VirtualService
      name: request-stress
