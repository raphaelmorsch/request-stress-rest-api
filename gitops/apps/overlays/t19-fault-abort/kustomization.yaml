apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

patches:
  - path: virtualservice-abort.yaml
    target:
      group: networking.istio.io
      version: v1beta1
      kind: VirtualService
      name: request-stress
